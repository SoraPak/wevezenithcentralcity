{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && rm -rf dist && mkdir -p dist && cp -r .nuxt/dist/client/* dist/",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clear-cache": "rm -rf node_modules/.cache && npm run build"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "swiper": "^11.1.14",
    "vue": "latest",
    "vue-router": "latest"
  }
}
